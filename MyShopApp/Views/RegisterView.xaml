<UserControl x:Class="MyShopApp.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyShopApp.Views"
             mc:Ignorable="d" Background="RoyalBlue" Height="278" Width="419">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Register" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <DockPanel>
                <TextBlock Text="Username: " VerticalAlignment="Center"/>
                <TextBox Text="{Binding Username}" Margin="10"/>
            </DockPanel>
            <DockPanel>
                <TextBlock Text="Password: " VerticalAlignment="Center"/>
                <TextBox Text="{Binding Password}" Margin="10"/>
            </DockPanel>
            
            <Button Content="Register" Command="{Binding RegisterCommand}" Margin="10"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
